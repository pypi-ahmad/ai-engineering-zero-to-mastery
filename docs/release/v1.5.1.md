# Release v1.5.1 (2026-07-03)

This patch release is polish focused: consistent commands and a clearer beginner-first front door.

## Highlights

- Overhauled the root `README.md` to be a better beginner entry point:
  - purpose, audience, outcomes
  - recommended study order + navigation
  - curated references with short descriptions
  - troubleshooting pointers to the right docs
- Standardized `uv sync` commands to recommend `--frozen` for reproducible installs.

## How To Verify

```bash
uv venv --python 3.12
source .venv/bin/activate
uv sync --frozen --group dev
python3 scripts/validate_curriculum.py
uv run pytest -q
```

