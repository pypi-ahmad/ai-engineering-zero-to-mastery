# Release v1.5.3 (2026-07-03)

Final polish release focused on repo-wide quality gates and beginner setup completeness.

## Highlights

- Added notebook markdown link validation to `scripts/validate_curriculum.py` (prevents broken “read this next” links inside notebooks).
- Filled release-readiness gaps:
  - `CHANGELOG.md` now includes a v1.5.2 entry
  - `docs/release/README.md` indexes v1.5.2
- Improved Windows setup instructions (PowerShell activation block) in `docs/setup-and-troubleshooting.md`.

## How To Verify

```bash
uv venv --python 3.12
source .venv/bin/activate
uv sync --frozen --group dev
python3 scripts/validate_curriculum.py
uv run pytest -q
```

