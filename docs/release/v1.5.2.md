# Release v1.5.2 (2026-07-03)

This patch release focuses on release-quality polish: clearer release navigation and a properly ordered changelog.

## Highlights

- Reordered `CHANGELOG.md` to be latest-first and added missing historical entries (v1.0.0/v1.0.1).
- Added a release notes index at `docs/release/README.md` for easier navigation.
- Minor CI consistency: use `python3` for curriculum validation.

## How To Verify

```bash
uv venv --python 3.12
source .venv/bin/activate
uv sync --frozen --group dev
python3 scripts/validate_curriculum.py
uv run pytest -q
```

