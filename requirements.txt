# NOTE: Prefer `uv sync --frozen` (see README) for deterministic installs via `uv.lock`.
# This file is kept as a lightweight fallback for users who must use pip.

# Core scientific Python + notebook runtime (Lessons 1-3)
numpy
pandas
matplotlib
seaborn
scikit-learn
scipy
xgboost
jupyter
ipykernel
requests
pyyaml
joblib
networkx

# Optional extras (install only when needed)
# torch
# torchvision
# pillow
# transformers
# diffusers
# accelerate
# datasets
# peft
# trl
# gymnasium
# fastapi
# uvicorn
# pydantic
# mlflow
# wandb
